---
dnsmasq_paths:
  Debian:
    config: /etc/dnsmasq.conf
    conf_d: /etc/dnsmasq.d
    zones:  /etc/dnsmasq.d/hosts
  RedHat:
    config: /etc
    conf_d: /etc/dnsmasq.d
    zones:  /etc/dnsmasq.d/hosts
